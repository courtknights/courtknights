# e2e — Acceptance test workspace

This workspace is the future home of Playwright acceptance tests for CourtKnights.
End-to-end tests will cover critical user journeys across the full stack.

Tests in this workspace are run exclusively by CI/CD and are not wired into any
local Makefile target at this stage. Integration with CI is deferred to
`FEATURE_ci_checks`.
