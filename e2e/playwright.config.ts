// Placeholder Playwright configuration — not yet functional.
// This file will be completed as part of FEATURE_ci_checks.
import { defineConfig } from '@playwright/test';

export default defineConfig({
  testDir: './tests',
});
