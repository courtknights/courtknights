# FEATURE_repo_workspaces — Task Breakdown

- **Status:** draft
- **Feature ID:** FEATURE_repo_workspaces
- **Generated by:** AI agent
- **Validated by:** <!-- human validator -->
- **Date:** 2026-03-23

---

## Rules

- Each task must be **atomic** — completable in a single PR.
- Each task must have a clear **done condition**.
- Tasks are ordered by dependency: complete top to bottom.
- Once approved, each task is created as a GitHub Issue with label `agent-task`.

---

## Tasks

### TASK-001: Remove stale artefacts from the repository root

- **GitHub Issue:** #42
- **Depends on:** none
- **Layer(s):** repository
- **Description:**
  Delete the empty directories left over from a previous incomplete restructuring attempt:
  `api/`, `ui/`, and `services/`. These contain no files and must be removed before the
  target workspace layout is established.
- **Done when:**
  - [ ] `api/`, `ui/`, and `services/` directories no longer exist at the repository root.
  - [ ] `git status` shows no untracked or deleted files other than those introduced by this task.
  - [ ] `make build` and `make test` pass.

---

### TASK-002: Move Go source tree into `api/`

- **GitHub Issue:** #43
- **Depends on:** TASK-001
- **Layer(s):** repository, api
- **Description:**
  Move the Go source tree from the repository root into a new `api/` directory:
  - Move `cmd/` → `api/cmd/`
  - Move `internal/` → `api/internal/`
  - Move `go.mod` → `api/go.mod`
  - Move `go.sum` → `api/go.sum`
  - Update `.gitignore`: replace `build/` entry with `api/build/`.
  The module path in `api/go.mod` remains `github.com/courtknights/courtknights` —
  no import paths change.
- **Done when:**
  - [ ] `cmd/`, `internal/`, `go.mod`, and `go.sum` exist only under `api/`.
  - [ ] Module path in `api/go.mod` is `github.com/courtknights/courtknights`.
  - [ ] `cd api && go build ./cmd/server` compiles without errors.
  - [ ] `cd api && go test ./...` passes.
  - [ ] `.gitignore` references `api/build/` instead of `build/`.

---

### TASK-003: Move `db/` to a top-level workspace

- **GitHub Issue:** #44
- **Depends on:** TASK-001
- **Layer(s):** repository, db
- **Description:**
  Move the database artefacts from their current location to a top-level `db/` workspace:
  - Move `db/schema/` → `db/schema/` (already at root, no path change needed if `db/`
    is currently at root; verify current location and move accordingly).
  - Move `db/migrations/` → `db/migrations/`.
  - Add `db/README.md` describing the workspace purpose and the `golang-migrate` convention
    for file naming (`{version}_{description}.up.sql` / `.down.sql`).
  - Add `db/Makefile` with `migrate`, `rollback`, and `status` targets using the
    `golang-migrate` CLI as specified in `02_architecture.md`.
- **Done when:**
  - [ ] `db/schema/` and `db/migrations/` exist at the repository root level.
  - [ ] `db/Makefile` contains `migrate`, `rollback`, and `status` targets.
  - [ ] `db/README.md` exists and documents the workspace and migration naming convention.
  - [ ] No references to the old `db/` path remain in any file outside `db/`.

---

### TASK-004: Add `go.work` at the repository root

- **GitHub Issue:** #45
- **Depends on:** TASK-002
- **Layer(s):** repository, api
- **Description:**
  Initialise a Go workspace at the repository root referencing the `api/` module:
  - Run `go work init ./api` from the repository root to generate `go.work` and
    `go.work.sum`.
  - Verify that `go build ./api/cmd/server` and `go test ./api/...` work from the
    repository root.
  - Commit both `go.work` and `go.work.sum`.
- **Done when:**
  - [ ] `go.work` exists at the repository root with `use ./api`.
  - [ ] `go.work.sum` exists and is committed.
  - [ ] `go build ./api/cmd/server` succeeds from the repository root.
  - [ ] `go test ./api/...` passes from the repository root.

---

### TASK-005: Replace root `Makefile` with workspace dispatcher

- **GitHub Issue:** #46
- **Depends on:** TASK-002, TASK-003, TASK-004
- **Layer(s):** repository
- **Description:**
  Replace the current root `Makefile` (which contains Go commands directly) with a
  dispatcher that delegates to workspace Makefiles:
  - Create `api/Makefile` by moving the current root `Makefile` content verbatim,
    adjusting paths to be relative to `api/` (`CMD_SERVER := ./cmd/server`,
    `BUILD_DIR := build`). All `go` commands in `api/Makefile` run without `go.work`
    redirection.
  - Create `web/Makefile` with `install`, `start`, `build`, and `test` targets wrapping
    the corresponding `npm` commands.
  - Rewrite the root `Makefile` as a dispatcher using `$(MAKE) -C <workspace>` for all
    targets, as specified in `02_architecture.md`.
- **Done when:**
  - [ ] `api/Makefile` exists and all targets (`build`, `run`, `test`, `test-int`,
        `test-all`, `lint`, `fmt`, `clean`) work when run from `api/`.
  - [ ] `web/Makefile` exists with `install`, `start`, `build`, and `test` targets.
  - [ ] Root `Makefile` contains only dispatcher targets; no direct Go or npm commands.
  - [ ] `make build` from the repository root compiles the backend binary.
  - [ ] `make test` from the repository root passes all unit tests.
  - [ ] `make web-test` from the repository root runs the frontend test suite.

---

### TASK-006: Scaffold `infra/` and `e2e/` workspaces

- **GitHub Issue:** #47
- **Depends on:** TASK-001
- **Layer(s):** repository
- **Description:**
  Create the minimum structure for the two future workspaces:
  - `infra/README.md` — describes the workspace as the future home of deployment
    manifests and Bash scripts.
  - `infra/.gitkeep` — ensures the directory is tracked by Git.
  - `e2e/README.md` — describes the workspace as the future home of Playwright
    acceptance tests; notes that tests are run exclusively by CI/CD.
  - `e2e/playwright.config.ts` — minimal placeholder config (not yet functional).
  - `e2e/package.json` — minimal placeholder listing `@playwright/test` as a dev
    dependency (not yet installed or wired into CI).
- **Done when:**
  - [ ] `infra/README.md` and `infra/.gitkeep` exist.
  - [ ] `e2e/README.md`, `e2e/playwright.config.ts`, and `e2e/package.json` exist.
  - [ ] Neither workspace is wired into any Makefile target or CI job.

---

### TASK-007: Update documentation and internal references

- **GitHub Issue:** #48
- **Depends on:** TASK-002, TASK-003, TASK-004, TASK-005, TASK-006
- **Layer(s):** repository
- **Description:**
  Update every document that references the old repository structure:
  - `CLAUDE.md`: update the repository structure diagram, the command table, and the
    Go conventions section to reflect `api/` paths.
  - `docs/decisions/ADR-008_layered-architecture.md`: update the package layout diagram
    (`internal/` → `api/internal/`).
  - `docs/testing/strategy.md`: verify `make test` / `make test-int` descriptions remain
    accurate (root delegation is transparent; no path changes expected).
  - `docs/testing/regression_map.md`: update any path references.
  - `README.md`: update the getting-started section and directory overview.
  - Confirm no remaining references to the old flat Go paths exist in any `.md` file
    under `docs/`.
- **Done when:**
  - [ ] `CLAUDE.md` repository structure diagram matches the target layout.
  - [ ] `CLAUDE.md` command table is accurate for the new Makefile structure.
  - [ ] `ADR-008` package layout diagram references `api/internal/`.
  - [ ] `README.md` getting-started section is accurate.
  - [ ] `grep -r "internal/" docs/` returns only valid references (i.e. `api/internal/`).
  - [ ] `make build` and `make test` pass after all documentation edits.

---
